require 'cucumber/core/test/timer'

module Cucumber
  module Core
    module Test
      describe Timer do
        it "would be slow to test" do
          # so we won't
        end
      end
    end
  end
end
